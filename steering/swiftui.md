# SwiftUI Conventions

- Match the existing project's state-management pattern.
- Reuse existing components before creating new ones.
- Respect the project's minimum iOS deployment target and availability requirements.
- Keep views focused on presentation and user interaction when ViewModels/services are already used.
- Preserve existing animation, navigation, and accessibility conventions.
- Use bindings only when the child view needs to edit parent-owned state.
